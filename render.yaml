services:
  - type: web
    name: cyberinsight-ai-agent
    env: python
    buildCommand: "cd agents && pip install -r requirements.txt"
    startCommand: "cd agents && python scheduler.py --interval 60"
    plan: free
